# Student-Score-Calculator
This is a simple Python project that takes a student's name and their scores in three subjects, stores them in a list, and calculates the total and average using basic operations and the len() function.The program then displays the results in a clear and formatted way.
